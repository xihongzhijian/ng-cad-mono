import {FloatingDialogTitleDirective} from "./floating-dialog-title.directive";

describe("FloatingDialogTitleDirective", () => {
  it("should create an instance", () => {
    const directive = new FloatingDialogTitleDirective();
    expect(directive).toBeTruthy();
  });
});
