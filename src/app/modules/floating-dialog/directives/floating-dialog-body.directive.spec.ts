import {FloatingDialogBodyDirective} from "./floating-dialog-body.directive";

describe("FloatingDialogBodyDirective", () => {
  it("should create an instance", () => {
    const directive = new FloatingDialogBodyDirective();
    expect(directive).toBeTruthy();
  });
});
