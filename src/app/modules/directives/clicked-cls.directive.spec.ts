import {ClickedClsDirective} from "./clicked-cls.directive";

describe("ClickedClsDirective", () => {
  it("should create an instance", () => {
    const directive = new ClickedClsDirective();
    expect(directive).toBeTruthy();
  });
});
