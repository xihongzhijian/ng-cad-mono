import {DataListItemsDirective} from "./data-list-items.directive";

describe("DataListItemsDirective", () => {
  it("should create an instance", () => {
    const directive = new DataListItemsDirective();
    expect(directive).toBeTruthy();
  });
});
