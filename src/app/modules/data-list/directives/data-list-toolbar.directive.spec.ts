import {DataListToolbarDirective} from "./data-list-toolbar.directive";

describe("DataListToolbarDirective", () => {
  it("should create an instance", () => {
    const directive = new DataListToolbarDirective();
    expect(directive).toBeTruthy();
  });
});
